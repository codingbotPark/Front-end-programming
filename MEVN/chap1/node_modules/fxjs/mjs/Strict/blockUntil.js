import curry from "./curry.js";
import go1 from "./go1.js";
export default curry(function blockUntil(f, predicate) {
  var block = false;
  return function () {
    if (block) return;
    block = true;
    var res = f.apply(void 0, arguments);
    go1(predicate(res), function () {
      return block = false;
    }).finally(function () {
      return block = false;
    });
    return res;
  };
});
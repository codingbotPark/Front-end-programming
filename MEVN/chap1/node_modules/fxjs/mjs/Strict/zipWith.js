import _toConsumableArray from "@babel/runtime-corejs3/helpers/toConsumableArray";
import zipL from "../Lazy/zipL.js";
import curry from "./curry.js";
import map from "./map.js";
export default curry(function zipWith(f) {
  for (var _len = arguments.length, iterables = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    iterables[_key - 1] = arguments[_key];
  }

  return map(function (group) {
    return f.apply(void 0, _toConsumableArray(group));
  }, zipL.apply(void 0, iterables));
});
import curry from "./curry.js";
import zipL from "../Lazy/zipL.js";
import takeAll from "./takeAll.js";
import go from "./go.js";
import apply from "./apply.js";
export default curry(function zip() {
  for (var _len = arguments.length, iters = new Array(_len), _key = 0; _key < _len; _key++) {
    iters[_key] = arguments[_key];
  }

  return go(iters, takeAll, apply(zipL), takeAll);
});